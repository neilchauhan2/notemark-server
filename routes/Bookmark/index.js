const route = require("express").Router();
const {} = require("../../controllers/Bookmark");

//create bookmark
route.post("/add", async (req, res) => {
  try {
  } catch (error) {
    throw error;
  }
});

// delete bookmark
